/**
 * Utility functions for code generation.
 */

/**
 * Format generated code for display (basic example).
 * @param code The generated code string.
 * @returns Formatted code.
 */
export function formatGeneratedCode(code: string): string {
  return code.trim();
}
